{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["lib/**"],
      "outputMode": "new-only"
    },
    "lint": {
      "outputs": []
    },
    "fix": {
      "outputs": []
    }
  }
}
